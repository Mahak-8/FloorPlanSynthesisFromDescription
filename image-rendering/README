# Image Rendering

# Python version - 3.5.2

# Modules can be installed using commands:
$ sudo pip install nltk
$ sudo pip install PIL
$ sudo pip install matplotlib
$ sudo pip install networkx

# Command to run:
$ python image_rendering.py
